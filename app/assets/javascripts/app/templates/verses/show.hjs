{{#if isEditing}}
  {{view App.EditVerseView}}
{{else}}
  {{App.selectedVerseController.verse.title}}
  {{App.selectedVerseController.verse.content}}
  <a href="#" {{action "showEdit"}}>Edit</a>
  <a href="#" {{action "destroyRecord"}}>Remove</a>
{{/if}}

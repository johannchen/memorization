<div class="card">
	<form>
		{{view Ember.TextArea rows="10" cols="40" valueBinding="typedContent" placeholder="type to memorize"}}
		<br />
		{{submitButton "Submit"}}
	</form>
	{{#if showResult}}
		<p>{{typedContent}}</p>	
	{{/if}}
</div>

